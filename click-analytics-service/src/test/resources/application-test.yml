server:
  port: 8084

spring:
  application:
    name: click-analytics-service
  cloud:
    config:
      enabled: false
  config:
    import: ""
  cassandra:
    contact-points: 127.0.0.1
    port: 9042
    keyspace-name: hopr
    local-datacenter: datacenter1
    schema-action: none
  kafka:
    bootstrap-servers: 127.0.0.1:9092
    consumer:
      group-id: click-analytics-service
